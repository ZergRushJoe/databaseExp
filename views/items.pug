extends layout

block content
    input(type="text" id="search")
    table
        thead
            tr
                th ID
                th Name
                th Quantity
        tbody
            -for(var i = 0;i< items.length;i++)
                tr
                    th #{items[i].id}
                    th #{items[i].name}
                    th #{items[i].quantity}